# Test Plan — Iteration {{iteration_id}} ({{feature_name}})

**Feature Slug**: `{{iteration_slug}}`
**Owner**: User (this file is user-maintained per Constitution §II)
**Status**: Initial baseline. Add scenarios as gaps surface.
**Date**: {{created_date}}

<!--
  sk Harness test-plan template. Output path:
  `.skspec/tests/test-plan-{{iteration_id}}.md`. This file captures
  **acceptance criteria** and **scenario-level tests** for the iteration.
  Implementation-level tests (unit / integration / e2e generated by the
  `qa` subagent) live alongside the source code, not here.

  Constitution §II: user owns `tests/`. The QA subagent can reference
  scenarios and may propose additions via `.prd-pending/`, but MUST NOT
  mutate this file directly.
-->

---

## 1. Test Environment

<!--
  ACTION REQUIRED: Replace these defaults with the real environment matrix.
-->

- **Host**: [e.g., macOS primary, Linux secondary]
- **Runtime**: [language version, frameworks]
- **External services**: [databases, APIs, mocks]
- **Filesystem**: [local, tmp, fixtures layout]

---

## 2. Smoke Suite (must pass before any commit lands)

<!--
  ACTION REQUIRED: Replace these scenario tables with real acceptance scenarios
  for this iteration. Keep the table format — `sk_harness.dag.parser` uses
  the SM-NN / INT-NN / OBS-NN IDs as acceptance references in tasks-{{iteration_id}}.md.
-->

### 2.1 [Feature Area 1]

| ID | Scenario | Expected |
|----|----------|----------|
| SM-01 | [User-facing action on a fresh setup] | [Observable outcome] |
| SM-02 | [Re-running the same action (idempotency)] | [No clobbering; safe no-op] |
| SM-03 | [Edge case input] | [Graceful handling] |

### 2.2 [Feature Area 2]

| ID | Scenario | Expected |
|----|----------|----------|
| SM-10 | [Primary happy path] | [Expected outcome] |
| SM-11 | [Variant or follow-up step] | [Expected outcome] |

### 2.3 [Feature Area 3]

| ID | Scenario | Expected |
|----|----------|----------|
| SM-20 | [End-to-end trivial case] | [Expected outcome] |
| SM-21 | [Parallelizable variant] | [Expected outcome] |

<!-- Add SM-30, SM-40, SM-50... per feature area. -->

---

## 3. Observability Suite

Scenarios that verify the decision-log, status reporting, and any
user-facing telemetry. Per Constitution §IV, observability is non-optional.

### 3.1 Decision Log Coverage

| ID | Scenario | Expected |
|----|----------|----------|
| OBS-01 | [Every significant action] | [Has at least one `decision-log.md` entry] |
| OBS-02 | [State transition of interest] | [Logged with before/after rationale] |

### 3.2 Status Reporting

| ID | Scenario | Expected |
|----|----------|----------|
| OBS-10 | `/sk.status` after fresh init | [Initial state printed] |
| OBS-11 | `/sk.status` mid-iteration | [Task counts, current batch, last decision] |

---

## 4. Integrity & Edge Cases

### 4.1 Filesystem Hand-Edits (user editing during pause)

| ID | Scenario | Expected |
|----|----------|----------|
| INT-01 | User edits `prd/spec-{{iteration_id}}.md` while paused | TPM next loop reads new content; emits ERD review if material |
| INT-02 | User edits `.task/tasks-{{iteration_id}}.md` directly | Sidecars resynced on next loop; diff logged |
| INT-03 | User deletes a snapshot file | Tolerated; affected task's restore path unavailable; logged |

### 4.2 Concurrency Bounds

| ID | Scenario | Expected |
|----|----------|----------|
| INT-10 | `max_concurrency=N` with fewer ready tasks | Dispatch count equals `min(ready, N)`; no padding |
| INT-11 | Non-idempotent task fails on retry | Auto-restore from snapshot if available; else escalate |

### 4.3 Schema Versioning

| ID | Scenario | Expected |
|----|----------|----------|
| INT-20 | `.skspec/.version` mismatch with running sk binary | Warn on first command; refuse `/sk.go` on incompatible bump |

---

## 5. Self-Bootstrap Verification

<!--
  Optional section. Use this when the iteration is building sk itself
  or any tool whose final acceptance is "the iteration can run its own
  loop using the output of the iteration." Remove if not applicable.
-->

The strongest acceptance test for some iterations is that the tool built
during iteration {{iteration_id}} can be used to carry out iteration
`{{iteration_id}}` retroactively. Specifically:

- [ ] [First self-referential smoke check]
- [ ] [Second self-referential smoke check]
- [ ] No file in `.skspec/` was lost during the verification

---

## 6. Out of Scope (this iteration)

<!--
  ACTION REQUIRED: List scenarios explicitly deferred to later iterations,
  with pointers to where they will land.
-->

Tests for future iterations (e.g., [feature X], [feature Y]) are deferred.
Add scenarios here when those iterations land.

---

**Maintenance**: When a regression escapes to production usage, add a
numbered scenario above (e.g., `SM-XX` or `INT-XX`) referencing the
gotchas entry that captured the root cause.
